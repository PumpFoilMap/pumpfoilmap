service: pumpfoilmap-backend
frameworkVersion: '3'

plugins:
  - serverless-offline
  - serverless-esbuild
  - serverless-dynamodb-local

provider:
  name: aws
  runtime: nodejs22.x
  region: eu-west-1
  stage: dev
  httpApi:
    cors: true
  environment:
    TABLE_SPOTS: ${self:service}-${sls:stage}-Spots
    ADMIN_TOKEN: ${env:ADMIN_TOKEN, 'dev'}
    ADMIN_MAIL: ${env:ADMIN_MAIL, ''}
    # Propagate local toggles into Lambda env
    USE_INMEMORY: ${env:USE_INMEMORY, ''}
    DYNAMODB_ENDPOINT: ${env:DYNAMODB_ENDPOINT, ''}
    PFM_DEBUG: ${env:PFM_DEBUG, ''}
    CAPTCHA_PRIVATE_KEY: ${env:CAPTCHA_PRIVATE_KEY, ''}

  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:DeleteItem
          Resource:
            - Fn::GetAtt: [SpotsTable, Arn]
        - Effect: Allow
          Action:
            - ses:SendEmail
            - ses:SendRawEmail
          Resource: '*'

functions:
  getSpots:
    handler: src/handlers/getSpots.handler
    events:
      - httpApi:
          path: /spots
          method: get

  postSpot:
    handler: src/handlers/postSpot.handler
    events:
      - httpApi:
          path: /spots
          method: post

  submitSpot:
    handler: src/handlers/submitSpot.handler
    events:
      - httpApi:
          path: /spots/submit
          method: post



  adminListPending:
    handler: src/handlers/adminListPending.handler
    events:
      - httpApi:
          path: /admin/spots/pending
          method: get

  adminUpdateSpot:
    handler: src/handlers/adminUpdateSpot.handler
    events:
      - httpApi:
          path: /admin/spots/{id}
          method: patch

  adminDeleteSpot:
    handler: src/handlers/adminDeleteSpot.handler
    events:
      - httpApi:
          path: /admin/spots/{id}
          method: delete

  adminListSpots:
    handler: src/handlers/adminListSpots.handler
    events:
      - httpApi:
          path: /admin/spots
          method: get

  adminCheckMd5:
    handler: src/handlers/adminCheckMd5.handler
    events:
      - httpApi:
          path: /admin/check-md5
          method: post

  captchaGenerate:
    handler: src/handlers/captchaGenerate.handler
    events:
      - httpApi:
          path: /captcha
          method: get

  captchaVerify:
    handler: src/handlers/captchaVerify.handler
    events:
      - httpApi:
          path: /captcha/verify
          method: post



resources:
  Resources:
    SpotsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.TABLE_SPOTS}
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: spotId
            AttributeType: S
        KeySchema:
          - AttributeName: spotId
            KeyType: HASH

custom:
  esbuild:
    bundle: true
    minify: false
    sourcemap: true
    target: node22
    platform: node
    external:
      - aws-sdk
      - svg-captcha
  dynamodb:
    stages:
      - dev
    start:
      port: 8000
      docker: true
      migrate: true
      seed: true
      inMemory: true
    seed:
      test:
        sources:
          - table: ${self:provider.environment.TABLE_SPOTS}
            sources: [./seeds/spots.json]
